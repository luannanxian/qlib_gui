/**
 * MSW Request Handlers
 *
 * Mock API endpoints for development
 */

import { http, HttpResponse, delay } from 'msw';

export const handlers = [
  // Placeholder - will add handlers when implementing mock features
];
